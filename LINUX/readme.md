To lauch the code you just have to make :
  -cd {folder where the file is}
  -npm install --build-from-source
  -node server.js

go on a browser on anything but connected on the same wifi :
    -go on http://{IP}:7000

to know you IP adress :
    -ifconfig
and see your IPV4 adress
